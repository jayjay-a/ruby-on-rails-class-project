<%= form_with(model: user_group, local: true) do |form| %>
  <% if user_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_group.errors.count, "error") %> prohibited this user_group from being saved:</h2>

      <ul>
      <% user_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.all, :id, :formatted_name %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.collection_select :user_id, Group.all, :id, :name %>
  </div>x

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
